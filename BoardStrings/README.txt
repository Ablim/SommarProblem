The solution is build with F# and .NET Core 2.

It is easy to build and run.

To build the solution use:
	dotnet build
	
To run the solution use:
	dotnet run BoardStrings.fsproj
	
To test the example use:
	cat Input.txt | dotnet run BoardStrings.fsproj